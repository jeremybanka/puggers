[package]
name = "puggers"
version.workspace = true
edition = "2024"
description = "CLI for converting HTML to Pug."
license.workspace = true
repository.workspace = true

[[bin]]
name = "puggers"
path = "src/main.rs"

[dependencies]
puggers-core = { version = "0.1.1", path = "../puggers-core" }
